Fork this repository using `fork`.

use `git clone (repository link)` to clone the files.

if you want local folder up to date use `git pull`.

use `git add .`
    `git commit`
    `git push`  to push the changes into remote repository.



